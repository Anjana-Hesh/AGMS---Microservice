server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

# Work the Scheduled tasks
spring:
  main:
    allow-bean-definition-overriding: true

sensor:
  external-api-url: http://104.211.95.241:8080/api
  fetch-rate: 10000 # 10 seconds